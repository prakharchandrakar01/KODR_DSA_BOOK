//Find surface area of sphere
let radius = Number(prompt("Enter the radius of the sphere:"));
let surfaceArea = 4 * Math.PI * radius * radius ;
console.log("The surface area of the sphere is: ", surfaceArea);